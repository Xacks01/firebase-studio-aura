# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Local Environment Variables
# IMPORTANT: If you create a .env file with sensitive API keys (like Firebase config),
# ensure it's listed here and NEVER commit it.
.env*.local
# .env # Uncomment this if your .env file contains secrets

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
# We want to keep .vscode/settings.json as it's part of your project.
.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace

# Next.js build output (already covered by .next but good for explicitness)
dist

# Turborepo (if you ever use it)
.turbo

# Dumps
*.stackdump

# Mac specific
.AppleDouble
.LSOverride

# Sourcemaps (often generated during build)
*.map